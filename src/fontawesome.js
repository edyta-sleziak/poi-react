import { library } from "@fortawesome/fontawesome-svg-core";
import { faGlobe, faList, faSearchLocation, faThumbsUp, faEdit, faInfo, faTrash } from "@fortawesome/free-solid-svg-icons";

library.add(
  faGlobe,
  faList,
  faSearchLocation,
  faThumbsUp,
  faInfo,
  faEdit,
  faTrash
);